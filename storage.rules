rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // PeerSchedule currently stores no user files. Keep Storage closed until
    // a feature defines authenticated paths, file types, and size limits.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
